import { AsinFilterPipe } from './asin-filter.pipe';

describe('AsinFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AsinFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
